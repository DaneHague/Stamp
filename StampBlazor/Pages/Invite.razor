@page "/invite/{token}"
@using StampBlazor.Components

<PageTitle>Collection Invitation - Stamp</PageTitle>

<InviteAcceptance InviteToken="@Token" />

@code {
    [Parameter] public string Token { get; set; } = string.Empty;
}